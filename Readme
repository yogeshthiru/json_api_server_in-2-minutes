Live mock up json api:

1. Create a repository in the git hub

2. Inside that create a db.json file and insert some json values

2. Open the url - https://my-json-server.typicode.com/userName/reponame
